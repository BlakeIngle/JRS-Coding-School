var arr1 = [0, 1, 1, 2, 3, 4, 5, 5, 6, 7, 8];
var arr2 = [0, 0, 1, 3, 3, 4, 5, 7, 7, 8, 9];


function mergeArrays(arr1, arr2) {
    var mergedArr = [];
    /* -------------------------------------------- */
    // write a function that will return an array
    // where the values in that array are all the 
    // values from two given arrays (in sorted order)

    /* -------------------------------------------- */
    return mergedArr;
}

console.log(mergeArrays(arr1, arr2))