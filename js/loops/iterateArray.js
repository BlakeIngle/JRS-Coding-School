var myArr = [3, 6, 32, 78, 9]

// look at every value in an array
// (iterate through the array)

console.log(myArr[0])

for (let i = 0; i < myArr.length; i++) {
    let element = myArr[i];
}

// for let of