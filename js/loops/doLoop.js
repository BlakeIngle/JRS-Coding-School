var i = 0;
var isAnswerFound = false;

while (!isAnswerFound) {
    // code
    var n = i * i;
    if (n >= 12000) {
        // answer is i-1 in this situation
        isAnswerFound = true;
    } else {
        i++;
    }
    continue;
    break;
    return;
}

// do while
do {
    console.log('do loops are easy')
} while (false)
