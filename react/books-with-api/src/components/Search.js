import React from 'react'

export default function Search() {

    // getContext -> http

    // function addNewBook() {
    //      http.addNewBook(book).then().catch();
    // }

    return (
        <div>
            search bar works
            {/* button -> on click -> addnewBook */}
        </div>
    )
}
